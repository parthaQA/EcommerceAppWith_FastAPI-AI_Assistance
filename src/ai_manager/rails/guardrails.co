import core
import avatars
import timing
import llm

flow main
  activate llm continuation
  activate off topic detection

flow user asked off topic question
  """The user is asking something unrelated to shopping and order support —
  such as general knowledge, coding help, personal advice, entertainment,
  or any topic that is not about finding products, prices, promotions,
  placing or managing an order, cart, checkout, returns, refunds,
  replacements, cancellations, or delivery issues."""
  user said something

flow off topic detection
  user asked off topic question
  bot say "I can only help with shopping-related questions, such as finding products, checking prices, managing your cart, or handling returns and refunds."